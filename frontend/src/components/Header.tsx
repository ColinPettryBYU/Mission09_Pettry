function Header() {
  return (
    // Header section that introduces the page.
    <header className="site-header">
      <h1>NCAA Basketball Teams</h1>
      <p>
        March Madness is here! Browse every college basketball team in the
        NCAA — check out their mascots and where they call home.
      </p>
    </header>
  );
}

export default Header;
